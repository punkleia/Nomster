<div class="booyah-box col-xs-10 col-xs-offset-1 col-md-6 col-md-offset 3">
  <h1>Add a Photo</h1>
  <%= simple_form_for @place do |pic| %>
    <%= pic.input :caption %>
    <%= pic.input :photo %>
   <br />
    <%= pic.submit 'Add Photo', class: 'btn btn-primary' %>
</div>